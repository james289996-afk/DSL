Python DSL for chip test by PyVISA
=================================

这个仓库实现了一个轻量 DSL（领域专用语言），用于通过 SCPI + PyVISA 组织芯片测试实验流程。

文件说明
--------

- `chiptest_dsl.py`：DSL 解释器 + 执行引擎 + PyVISA/Mock 后端
- `examples/chip_leakage.dsl`：芯片漏电流测试示例脚本
- `examples/multi_dut_batch.dsl`：多 DUT 批量冒烟测试示例
- `examples/on_fail_demo.dsl`：失败回调（ON_FAIL）示例脚本

DSL 语法
--------

注释：以 `#` 开头。

支持命令：

1. 连接设备

   `CONNECT <alias> "<visa_resource>"`

   示例：
   `CONNECT dmm "USB0::0x2A8D::0x1301::MY12345678::INSTR"`

2. 下发 SCPI 写命令

   `WRITE <alias> "<scpi_command>"`

   支持变量模板：
   `WRITE psu "VOLT {vdd}"`

3. 查询并保存结果

   `QUERY <var_name> FROM <alias> "<scpi_query>"`

   示例：
   `QUERY i_meas FROM dmm "MEAS:CURR:DC?"`

4. 变量赋值（Python 表达式）

   `LET <var> = <expression>`

   示例：
   `LET avg_i = sum(samples) / len(samples)`

5. 等待

   `WAIT <seconds_expression>`

6. 断言

   `ASSERT <expression> MESSAGE "<text>"`

   `MESSAGE` 可省略：
   `ASSERT avg_i < 1e-5`

7. 循环

   ```
   REPEAT <count_expression>
       ...commands...
   END
   ```

   循环中内置变量 `i`（从 0 开始）。

8. 输出

   `PRINT "<text with {vars}>"`
   或
   `PRINT <python_expression>`

9. 条件分支

   ```
   IF <condition_expression>
       ...commands...
   ELIF <condition_expression>
       ...commands...
   ELSE
       ...commands...
   END
   ```

   `ELIF` 与 `ELSE` 都可省略。

10. 重试块

    ```
    RETRY <attempts_expression> [DELAY <seconds_expression>]
        ...commands...
    END
    ```

    - 任意子命令抛错都会触发重试。
    - 内置变量：
      - `retry_attempt`：当前（或最终成功）尝试次数，从 1 开始
      - `last_error`：最近一次失败信息

11. 指标记录

    `METRIC <name> = <expression>`

    示例：
    `METRIC avg_leakage = avg_i`

12. 报告导出

    - `REPORT JSON "<path>"`
    - `REPORT CSV "<path>"`

    说明：
    - JSON 报告包含 `metrics + variables + generated_at`。
    - CSV 报告输出两列：`metric,value`。

13. 列表遍历

    ```
    FOR <var> IN <iterable_expression>
        ...commands...
    END
    ```

    说明：
    - 适合批量 DUT 测试。
    - 循环中内置变量 `loop_index`（从 0 开始）。

14. 失败回调

    ```
    ON_FAIL
        ...commands...
    END
    ```

    说明：
    - 当脚本执行出现异常时自动执行，用于安全收尾（如 `WRITE psu "OUTP OFF"`）。
    - 可在脚本中出现多次，后定义的会覆盖前定义的处理块。

运行方式
--------

1. Mock 模式（无仪器，先验证流程）

   `python3 chiptest_dsl.py examples/chip_leakage.dsl --mock --dump-vars`

   或批量 DUT 示例：
   `python3 chiptest_dsl.py examples/multi_dut_batch.dsl --mock --dump-vars`

2. 真机模式（需要 PyVISA + 驱动）

   `python3 chiptest_dsl.py examples/chip_leakage.dsl --dump-vars`

依赖建议
--------

真机模式建议安装：

- `pyvisa`
- 对应 VISA 后端（如 NI-VISA）或 `pyvisa-py`

注意
----

- DSL 表达式使用受限的 Python `eval` 环境（仅开放常见数学/类型函数）。
- `QUERY` 结果默认是字符串，若用于数值计算，请使用 `float(...)` 转换。
- 示例脚本会生成 `reports/chip_leakage.json` 和 `reports/chip_leakage.csv`。
- 批量示例会生成 `reports/lot_demo_01_summary.json` 和 `reports/lot_demo_01_summary.csv`。
